{
  "name": "electron-tesserae",
  "version": "0.0.1",
  "description": "",
  "main": "renderer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "rm -rf dist && npm run -s package-python && npm run -s package-electron && npm run -s package-cleanup",
    "package-python": "pyinstaller --clean --windowed --onefile --distpath dist-python --additional-hooks-dir pyinstaller-hooks tisapi/run_app.py",
    "package-electron": "electron-builder",
    "package-cleanup": "rm -rf build && rm -rf run_app.spec && rm -rf dist-python"
  },
  "build": {
    "appId": "edu.buffalo.tesserae",
    "productName": "Tesserae",
    "asar": true,
    "asarUnpack": [
      "**/*.node"
    ],
    "mac": {
      "category": "public.app-category.utilities"
    },
    "files": [
      "renderer.js",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "dist-python/",
        "to": "app/dist-python",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "frontend/",
        "to": "app/frontend",
        "filter": [
          "**/*"
        ]
      }
    ]
  },
  "eslintConfig": {
    "env": {
      "es6": true,
      "node": true
    },
    "parserOptions": {
      "ecmaVersion": 8
    }
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "command-exists": "^1.2.9",
    "find-process": "^1.4.3",
    "gunzip-maybe": "^1.4.2",
    "ini": "^1.3.5",
    "mkdirp": "^1.0.4",
    "mongodb": "^3.5.7",
    "ps-tree": "^1.2.0",
    "tar-fs": "^2.1.0",
    "yauzl": "^2.10.0"
  },
  "devDependencies": {
    "electron": "^6.0.10",
    "electron-builder": "^21.2.0",
    "eslint": "^7.0.0"
  }
}
